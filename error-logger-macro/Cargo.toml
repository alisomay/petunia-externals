[package]
name = "error-logger-macro"
version = "0.1.0"
edition = "2021"
description = "A proc macro for automatic error logging"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "visit-mut"] }